{% extends 'base.html.twig' %}

{% block title %}Index {% endblock %}

{% block body %}

<section class="Categories">

    {% for category in categories %}
            
        <a href="{{ path('category_show', {'title' : category.Title }) }}" class="btn btn-primary" style="margin-left: 10px; margin-top: 15px;">{{category.Title}}</a>    

    {% endfor %}

    <a href="{{ path('addCategory') }}" class="btn btn-primary " style=" background-color:grey; margin-left: 1300px; margin-top: 0px;">Ajouter une catégorie</a>
    <a href="{{ path('book') }}" class="btn btn-primary " style=" background-color:grey; margin-left: 1700px; margin-top: 0px;">Précédent</a>
</section>


{% endblock %}
